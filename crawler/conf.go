package crawler

const (
	// EntryPoint is the starting point of the crawler
	EntryPoint = "https://espace.com.eg/"
	// NumberOfCrawlers is the number of parralel crawlers to be run at the same time
	NumberOfCrawlers = 0
	// DefaultImagePath is the defaut path for downloaded images
	DefaultImagePath = "images/"
)
